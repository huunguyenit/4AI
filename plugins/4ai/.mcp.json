{
  "mcpServers": {
    "4ai-fbo": {
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/fbo/server.mjs",
        "--hub",
        "${CLAUDE_PLUGIN_ROOT}"
      ],
      "command": "node",
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "FBO_DATA_ROOT": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
