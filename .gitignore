.4ai/cache/
.4ai/ledger/
.4ai/index/
*.local.json
logs/

# Tài liệu dự án của khách (khảo sát, biên bản, email). Dữ liệu khách — không commit.
.management/
ledger/

# Script nạp đồ thị do `graph build` sinh ra — dựng lại được từ JSONL, không commit.
.4ai/graph/

# Output tạm của render_review_report/get_review_dataset — CHỨA DỮ LIỆU KHÁCH THẬT (tên
# khách, mã nhân viên, nội dung UR). Từng bị git add nhầm (commit a713fdd) và lộ ra repo
# public — xem CHANGELOG. Không bao giờ commit.
.4ai/scratch/

# Không bao giờ dùng npm — dòng này là phòng thủ, không phải dự kiến.
node_modules/
package-lock.json
data/qlda.local.json

# Giấy phép đã kích hoạt trên MÁY NÀY (gắn device id) — không commit.
data/license.json
# Khoá ký giấy phép. Chỗ đúng của nó là ~/.4ai/keys/, dòng này là phòng thủ nếu ai đó
# lỡ sinh khoá vào trong repo.
*.pem
.cursor/mcp.json
targets.local.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
# Neo vào gốc repo (/graft/): graft/ trần khớp MỌI thư mục tên graft ở bất kỳ độ sâu,
# kể cả .claude/skills/graft/ — chính file graft init sinh ra để commit và chia sẻ.
/graft/


# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
