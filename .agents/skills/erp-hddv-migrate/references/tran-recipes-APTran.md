<!-- GENERATED BY 4AI — DO NOT EDIT.
     Source: assets/skills/erp/erp-hddv-migrate/references/tran-recipes-APTran.md (skill v2)
     Hub: D:\Fast Source\4AI · Regenerate: node tools/4ai.mjs sync -->

# APTran — HDDV Recipe (PN1 / m31$)

**File:** `App_Data/Controllers/Dir/APTran.xml`  
**Include:** `%InputInvoice.Include.APTran;` → `InputInvoice.APTran`  
**Bảng link HĐ:** `h31$` (qua `h&Tag;$` trong `InputInvoice.ent`)  
**Detail/Tax:** `d31`, `r30`

## Entity SQL (nhóm chuẩn — KHÔNG dùng II*)

| Event | Entity |
|-------|--------|
| Inserting | `&InputInvoiceCheck;` |
| Inserted | `&InputInvoiceInsert;` |
| Updated | `&InputInvoiceUpdate;` |
| Deleted | `&InputInvoiceDelete;` |

## @script — R2SP222 (không List)

```xml
&InputInvoiceScriptDeclare;
&InputInvoiceScriptWarning;
&InputInvoiceInsert;   <!-- hoặc Update -->
&InputInvoiceScriptQuery;
```

**Inserted đặc biệt:** sau `ScriptQuery` còn block LOITP (`@$warning` tháng HĐ nội bộ), `zclkts`/`zclkcc`, `commit/try/catch` — **giữ nguyên**, không đổi thứ tự nghiệp vụ gốc. `CommandShowWarningMessage` ở cuối Inserted.

**Updated:** `ScriptQuery` trước block LOITP + `CommandShowWarningMessage`.

## Extender kèm theo (giữ, không phải List)

- `FlowMultiVoucher` + `FlowMultiScript`
- `DPScript` + `DPWhenBeforeInsert` / `DPPost*`
- **Không** `PostScript`

## Script II bắt buộc

`ImportXmlIdentity`, `IIActiveVoucherScript`, `IICloseVoucherScript`, `ImportXmlScript`, `ImportXmlScriptShowForm`, `InputInvoiceRefreshGridScript`, `IIInsertDataScript`, `FlowMultiScript`

## Lưu ý migrate

- Inserting dùng `&zcCommandCheckDetailInputVATInvoice;` (không phải `CommandCheckDetailInputVATInvoice` chuẩn).
- Transaction `begin try` / `commit` bọc Inserted — entity II **ngoài** CDATA.
