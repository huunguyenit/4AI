---
description: Sinh báo cáo hạn UR bằng `4ai report` rồi phân tích các UR trạng thái DD. Có mã dự án → 1 dự án. Bỏ trống → TOÀN BỘ.
---
<!-- GENERATED BY 4AI — DO NOT EDIT.
     Source: assets/commands/project-mgmt/pm-review.md (v8)
     Hub: D:\Fast Source\4AI · Regenerate: node tools/4ai.mjs sync -->

## Việc cần làm

Rà soát yêu cầu cho: **$ARGUMENTS**

Giao [pm-deadline-review]. Tham số là mã dự án (`nbdmda.ma_da`).

**Có mã dự án** → `node tools/4ai.mjs report --project $ARGUMENTS` rồi `serve /review/<MA_DA>`.

**Bỏ trống** → `node tools/4ai.mjs report` rồi `serve /review`. Toàn bộ dự án `PM01` đứng tên LTQL — không chỉ dự án nói tới gần đây.

**Không có shell** (chat, Cowork — không chạy được `node`) → gọi tool `render_review_report`
(`project` bỏ trống = toàn bộ). Nó dựng đúng bộ file đó bằng chính code của `4ai report` và trả
về `ddUR[]` để phân tích ngay tại chỗ. Bề mặt đó cũng không có sub-agent, nên tự làm theo kịch
bản [pm-deadline-review] thay vì hứa là sẽ giao cho nó.

Sau khi HTML có: **chỉ phân tích UR `trang_thai = DD`** (tài liệu đầu vào, ảnh hưởng, phân việc, đề xuất XN/TA/KL). `XN`/`TH` chỉ xem hạn trên báo cáo.

Không viết SQL danh sách UR, không ghép payload, không dùng `plan_report`.
**Không bao giờ tự dựng báo cáo từ `get_review_dataset`** — đó là dữ liệu thô: báo cáo tự chế
bỏ qua validate payload, không nằm trong ledger, và luôn kéo theo việc phân tích cả `XN`/`TH`.

## Ràng buộc

- Chỉ **đề xuất** thay đổi trạng thái. Soạn sẵn câu lệnh, chờ PM xác nhận rồi mới chạy.
- Không SELECT `nbdmda.server`, `xuser`, `xpass`, `db_sys`.
- Không tự gửi báo cáo cho ai khác.
- Thiếu dữ kiện thì hỏi PM, không suy đoán để lấp chỗ trống.
