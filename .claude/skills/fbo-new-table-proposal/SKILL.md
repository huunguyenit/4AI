---
name: fbo-new-table-proposal
description: Yêu cầu nhắc tạo bảng/thêm cột thì cấp đặc tả `ddl` để tools/lib/ddl.mjs sinh SQL — đừng tự viết cú pháp. Màn hình FBO đã có sẵn thì khai luongDuLieu, không tạo bảng mới.
---
<!-- GENERATED BY 4AI — DO NOT EDIT.
     Source: assets/skills/fbo-xml/fbo-new-table-proposal.md (v4)
     Hub: D:\FBO-Shared\mcp\4AI · Regenerate: node tools/4ai.mjs sync -->

# Proposing new tables and columns

## Vì sao

Lập trình viên thiết kế bảng từ đầu thì lệch chuẩn FBO, thiếu cột audit, đặt tên trùng
sản phẩm chuẩn, hoặc quên partition rồi vài năm sau bảng phình không cứu được. PM chặn
những cái đó bằng một ghi chú đúng chuẩn đính vào UR.

Quy ước: `data/fbo-ddl.json`.

## PM làm gì và không làm gì

**Làm**: đề xuất tên bảng, chỉ chứng từ chuẩn để sao chép, ghi rõ aspx và controller, và cấp
**đặc tả `ddl` có cấu trúc** (tên bảng + danh sách cột + kiểu) để bộ sinh viết SQL.

**Không làm**: chạy bất kỳ lệnh DDL nào, tạo sẵn bảng trên DB khách, chốt số hiệu chứng từ
khi chưa quét DB App của chính dự án đó.

## KHÔNG tự viết SQL — cấp đặc tả, để bộ sinh viết

Đây là luật quan trọng nhất của skill này. Viết SQL bằng tay thì lúc đúng lúc sai: hai lượt
chạy thật ngày 2026-08-10 sinh ra văn xuôi thay vì script, gán tiền tố `zc` cho bảng trung
gian, thiếu dấu phẩy trước `CONSTRAINT`. Chỉ dẫn suông không sửa được chuyện đó.

Nên quy ước DDL nằm trong code: [tools/lib/ddl.mjs](../../../tools/lib/ddl.mjs). Payload cấp
trường `ddl` có cấu trúc, `report` gọi bộ sinh:

    "ddl": {
      "kind": "staging",              // staging | danh-muc | chung-tu-moi | them-cot
      "table": "Town",
      "pk": ["Inc"],
      "moTa": "…", "nguon": "TLKS … mục 3.1",
      "canhBao": ["TLKS ghi Nvarchar(24) — giữ nvarchar"],
      "columns": [
        { "name": "Inc",  "type": "int", "notNull": true, "note": "khóa chính" },
        { "name": "Name", "type": "nvarchar(24)" }
      ]
    }

Bộ sinh lo phần dễ sai: dấu phẩy, `CONSTRAINT PK_…`, canh lề, marker `-- DB:`, và **tự thêm
đủ 5 cột audit** cho `danh-muc`. Nó **từ chối** spec sai luật — `staging` mà đặt tên `zc…`,
`danh-muc` mà thiếu `zc`, cột thiếu `type`, số hiệu chứng từ không nằm trong danh sách số
trống đã quét. Lỗi hiện thẳng trên báo cáo kèm lý do.

Kiểu dữ liệu vẫn là việc của người: `nvarchar` cho tên/địa danh tiếng Việt (`varchar` làm mất
dấu mà không báo lỗi), `char` không kèm độ dài mặc định là `char(1)`. Nguồn ghi thiếu kiểu thì
đặt `"deXuatKieu": true` — script sẽ đánh dấu rõ chỗ đó là đề xuất.

## Màn hình đã có sẵn thì KHÔNG tạo bảng

Yêu cầu kiểu *"tạo tính năng sinh Hóa đơn dịch vụ từ bảng trung gian"* **không phải** yêu cầu
tạo chứng từ mới — `Hóa đơn dịch vụ` (HD1/ARTran) đã chạy sẵn trên FBO. Đừng cấp số hiệu mới,
đừng `AddTable`. Cái cần chốt là luồng dữ liệu, khai bằng `luongDuLieu`:

    "luongDuLieu": {
      "nguon": ["DEMO1_Mid.Cost (bảng trung gian Chi phí)", "Danh sách hóa đơn đầu vào"],
      "dich": { "manHinh": "Hóa đơn mua dịch vụ", "syscode": "PN1", "sysid": "APTran", "bang": "m31$" },
      "ghiChu": "TLKS mục 4.4 …"
    }

Chỉ dùng `kind: "chung-tu-moi"` khi nghiệp vụ thật sự chưa có màn hình nào trên FBO. Kiểm bằng
`resolve_vouchercode` trên chính program của khách trước khi kết luận là chưa có.

## Chưa có schema thì đi tìm, đừng bắt PM chép tay

Cột của bảng nguồn thường nằm sẵn trong TLKS đính kèm dự án (`sysfileinfo`, `controller='nbdmda'`,
`syskey=<ma_da>`) — tải file theo `file_enc`, đọc, lấy đúng bảng cột. Đừng viết `-- <cột theo
schema Samo>` rồi giao cho PM tự điền: đó là đẩy việc ngược lại người đọc báo cáo.

Tìm hết cách vẫn không có thì mới ghi rõ "CHƯA ĐỦ DỮ KIỆN" — và nói rõ đã tìm ở đâu.

## Ba loại bảng, ba bộ quy tắc khác nhau

| Loại | Đặt tên | Cột bắt buộc | Xem |
|---|---|---|---|
| Danh mục (DB App) | tiền tố `zc` | 5 cột audit | `danhMuc` |
| Chứng từ (DB App) | họ `c/m/d/i<nn>$` | theo chứng từ tham khảo | `chungTu` |
| Bảng trung gian (DB staging) | y hệt tên hệ nguồn | theo hệ nguồn, KHÔNG ép audit | `bangTrungGian` |

**Bảng trung gian** — dùng khi yêu cầu là "lấy dữ liệu từ hệ thống ngoài (Samo, đối tác...) về
phần mềm", KHÔNG phải danh mục hay chứng từ của FBO. Không áp `zc`, không ép 5 cột audit — bảng
chỉ cần mirror đúng cấu trúc hệ nguồn để ETL không mất dữ liệu. Ví dụ đã xác nhận: bảng `Town`
(vùng địa lý từ Samo về DB trung gian của DEMO1) — xem `fbo-ddl.json` → `bangTrungGian.verifiedExample`.

## Danh mục

Tiền tố `zc` — `z` đẩy xuống cuối khi sắp xếp, `c` là customize. Ví dụ danh mục tiến độ
thanh toán → `zcdmtdtt`.

Năm cột BẮT BUỘC với mọi bảng danh mục: `status`, `user_id0`, `user_id2`, `datetime0`,
`datetime2`. Kiểu dữ liệu và cột nghiệp vụ sao chép từ một danh mục chuẩn cùng loại trong
chính chương trình đó — không tự nghĩ ra kiểu.

## Chứng từ

KHÔNG thiết kế từ đầu. Luôn sao chép từ chứng từ chuẩn cùng dạng nghiệp vụ, rồi bỏ/thêm cột.
Bảng ánh xạ phân hệ → chứng từ tham khảo nằm ở `fbo-ddl.json` → `referenceVouchers`
(đã phân giải: `BN1/CPTran`, `HDA/SVTran`, `PNA/PVTran`, `PND/IRTran`, `PXB/ITTran`,
`DXA/SOTran`, `PO1/POTran`).

Họ bảng: `c<nn>$` config · `m<nn>$` master · `d<nn>$` detail · `i<nn>$` inquiry.
Ví dụ đầy đủ nhất là `HDA` → `c81$ / m81$ / d81$ / i81$`.

**Số hiệu** phải là số chưa từng xuất hiện trong DB App của **chính dự án đó**. Quét tên bảng
khớp `m__$%`, `d__$%`, `i__$%`, `c__$%`, bóc tập số đã dùng, chọn số ngoài tập. Số trống ở
chương trình này có thể đã dùng ở chương trình khác — không suy từ dự án khác sang.

**Partition** khi dự kiến nhập trên 20 phiếu/ngày. Mốc lấy từ `dmstt.ngay_gh1`/`ngay_gh2`,
không hardcode ngày. Mẫu script đầy đủ ở `fbo-ddl.json` → `chungTu.scriptTemplate`.

## Thêm cột

Cũng chỉ là gợi ý trong ghi chú UR. Checklist: nói rõ bảng và **mọi partition** của họ bảng
đó (không chỉ `$000000`); kiểu lấy theo cột tương đương ở chứng từ tham khảo; và nhắc kiểm
cột dự phòng sẵn có trước khi thêm cột mới — nhiều yêu cầu chỉ cần trưng dụng slot.

## Bẫy

- Cột dự phòng (`s1..s9`, `fcode*`, `fdate*`, `fqty*`, `fnote*`, `ma_td*`, `sl_td*`, `gc_td*`)
  **chỉ được coi là trống sau khi soi hết command SQL trong controller**. Trông rỗng không có
  nghĩa là chưa ai dùng.
- Tra mã chứng từ bằng `resolve_vouchercode`: `wcommand` cho menu và sysid, `dmct9` cho họ bảng.
  Hai nguồn độc lập — thiếu một nguồn vẫn có kết quả một phần, đừng coi đó là "không tồn tại".
- Mã chứng từ khác sysid controller: `HDA` → `SVTran`, `PND` → `IRTran`, `DXA` → `SOTran`.
  Đừng đoán sysid từ mã.


> Xem thêm: `fbo-controller-anatomy`, `fbo-sql-object-lookup`, `pm-deadline-review`
